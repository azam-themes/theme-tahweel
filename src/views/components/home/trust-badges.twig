{# ============================================
   Trust Badges Component - ثيم تحويل
   شارات الثقة والضمان لزيادة ثقة العميل
   ============================================ #}

<div class="trust-bar">
    <div class="container">
        <div class="trust-bar__grid">
            <div class="trust-bar__item">
                <div class="trust-bar__icon">
                    <i class="sicon-truck"></i>
                </div>
                <div class="trust-bar__text">
                    <h4>{{ trans('trust.free_shipping') }}</h4>
                    <p>{{ trans('trust.free_shipping_desc') }}</p>
                </div>
            </div>
            <div class="trust-bar__item">
                <div class="trust-bar__icon">
                    <i class="sicon-shield-check"></i>
                </div>
                <div class="trust-bar__text">
                    <h4>{{ trans('trust.secure_payment') }}</h4>
                    <p>{{ trans('trust.secure_payment_desc') }}</p>
                </div>
            </div>
            <div class="trust-bar__item">
                <div class="trust-bar__icon">
                    <i class="sicon-refresh"></i>
                </div>
                <div class="trust-bar__text">
                    <h4>{{ trans('trust.easy_return') }}</h4>
                    <p>{{ trans('trust.easy_return_desc') }}</p>
                </div>
            </div>
            <div class="trust-bar__item">
                <div class="trust-bar__icon">
                    <i class="sicon-headphones"></i>
                </div>
                <div class="trust-bar__text">
                    <h4>{{ trans('trust.support') }}</h4>
                    <p>{{ trans('trust.support_desc') }}</p>
                </div>
            </div>
        </div>
    </div>
</div>
